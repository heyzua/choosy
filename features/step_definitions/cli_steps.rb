# Setup
Given /^the CLI tool '([^']*)'$/ do |tool|
  pending
end

# Options
When /^I run it with no arguments$/ do
  pending
end

When /^I run it with the argument '([^']*)'$/ do |arguments|
  pending
end

# Output
Then /^I should see the current time, timezone at '([^']*)'$/ do |tz|
  pending
end

Then /^it should print out the current year$/ do
  pending
end

Then /^it should print out '([^']*)'$/ do |output|
  pending
end

Then /^it should match '([^']*)'$/ do |output|
  pending
end
